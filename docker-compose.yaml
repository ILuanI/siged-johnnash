version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - "80"
    environment:
      APP_NAME: "SIGED"
      APP_ENV: production
      APP_DEBUG: "false"
      APP_KEY: "base64:vZi6fPR+/CIFZljCVvIVDiD2aK9c47XMVonHIbW75So="
      APP_URL: "${COOLIFY_URL}"
      LOG_CHANNEL: stack
      LOG_LEVEL: error
      DB_CONNECTION: mysql
      DB_HOST: o04gwggs0400o0so0wk484oc
      DB_PORT: 3306
      DB_DATABASE: default
      DB_USERNAME: mysql
      DB_PASSWORD: "1sJUERkKwmNOzybjp1z26fT29yxxCps5McxU0pexTxPJW7IO0CbjOtmUIovvuQxE"
      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
      RUN_MIGRATIONS: "true"
